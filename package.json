{
  "name": "flower-sub",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "npm run build:worker && vite build",
    "preview": "vite preview",
    "build:worker": "node build-worker.mjs",
    "test:worker": "node test-worker.mjs",
    "test": "npm run test:worker && npm run build"
  },
  "dependencies": {
    "react": "^19.1.1",
    "react-dom": "^19.1.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^6.0.0",
    "vite": "^8.2.2"
  }
}
